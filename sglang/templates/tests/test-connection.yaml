apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sglang.fullname" . }}-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: busybox
      command: ['curl']
      args: ['{{ include "sglang.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
